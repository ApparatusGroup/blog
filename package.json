{
  "name": "ai-agents-blog",
  "version": "0.1.0",
  "scripts": {
    "vercel-build": "python -m venv .venv && .venv/bin/python -m pip install -r requirements.txt && .venv/bin/python -m src.main"
  }
}
